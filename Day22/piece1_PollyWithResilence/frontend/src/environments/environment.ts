export const environment = {
  production: false,
  // In development, the Angular dev server proxies /api/* to localhost:5051
  // via proxy.conf.json — no raw backend URL is needed here.
  apiBaseUrl: '/api',
};
